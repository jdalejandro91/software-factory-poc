"""Vulture whitelist — false positives from abstract method signatures."""

input_data  # BaseSkill.execute() abstract parameter
method_name  # structlog processor signature (logger, method_name, event_dict)
