import time

def main():
    service = "{{ service_name }}"
    owner = "{{ owner_team }}"
    print(f"Starting {service} (Owner: {owner})...")
    
    while True:
        print(f"[{service}] Working...")
        time.sleep(5)

if __name__ == "__main__":
    main()
